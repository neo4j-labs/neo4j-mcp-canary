{
  "manifest_version": "0.3",
  "name": "neo4j-mcp",
  "version": "0.1.1",
  "description": "Canary Model Context Protocol (MCP) server for Neo4j.",
  "author": {
    "name": "Neo4j",
    "url": "https://neo4j.com/"
  },
  "homepage": "https://github.com/neo4j-labs/neo4j-mcp-canary",
  "user_config": {
    "NEO4J_URI": {
      "type": "string",
      "title": "Connection URI",
      "description": "Connection URI (e.g., bolt://localhost:7687)",
      "required": true,
      "sensitive": false
    },
    "NEO4J_USERNAME": {
      "type": "string",
      "title": "Neo4j username",
      "description": "Neo4j username (e.g., neo4j)",
      "required": true,
      "sensitive": false
    },
    "NEO4J_PASSWORD": {
      "type": "string",
      "title": "Neo4j password",
      "description": "Neo4j password",
      "required": true,
      "sensitive": true
    },
    "NEO4J_DATABASE": {
      "type": "string",
      "title": "Database name",
      "description": "Database name (e.g., neo4j)",
      "required": false,
      "sensitive": false
    },
    "NEO4J_READ_ONLY": {
      "type": "boolean",
      "title": "Readonly flag",
      "description": "Set to true to disable write tools",
      "required": false,
      "sensitive": false
    },
    "NEO4J_TELEMETRY": {
      "type": "boolean",
      "title": "Telemetry",
      "description": "Set to false to disable telemetry (default true)",
      "required": false,
      "sensitive": false
    },
    "NEO4J_LOG_LEVEL": {
      "type": "string",
      "title": "Log Level",
      "description": "Log level (e.g., debug, info, notice, warning, error, critical, alert, emergency)",
      "required": false,
      "sensitive": false
    },
    "NEO4J_LOG_FORMAT": {
      "type": "string",
      "title": "Log Format",
      "description": "Log output format: text or json (default text)",
      "required": false,
      "sensitive": false
    },
    "NEO4J_SCHEMA_SAMPLE_SIZE": {
      "type": "string",
      "title": "Schema inference sample size",
      "description": "Number of nodes to sample for schema inference",
      "required": false,
      "sensitive": false
    },
    "NEO4J_MCP_EMBEDDING_PROVIDER": {
      "type": "string",
      "title": "Embedding provider",
      "description": "GenAI embedding provider for set-vector-property's text field / check-embedding-dimensions: openai, azure-openai, vertexai, or bedrock-titan; leave unset to disable both",
      "required": false,
      "sensitive": false
    },
    "NEO4J_MCP_EMBEDDING_CONFIGURATION": {
      "type": "string",
      "title": "Embedding provider configuration",
      "description": "Comma-separated key=value settings for the embedding provider above (e.g. token=sk-xxx,model=text-embedding-3-small) — required keys vary by provider",
      "required": false,
      "sensitive": true
    }
  },
  "server": {
    "type": "binary",
    "entry_point": "neo4j-mcp",
    "mcp_config": {
      "command": "${__dirname}${/}bin/neo4j-mcp",
      "env": {
        "NEO4J_URI": "${user_config.NEO4J_URI}",
        "NEO4J_USERNAME": "${user_config.NEO4J_USERNAME}",
        "NEO4J_PASSWORD": "${user_config.NEO4J_PASSWORD}",
        "NEO4J_DATABASE": "${user_config.NEO4J_DATABASE}",
        "NEO4J_READ_ONLY": "${user_config.NEO4J_READ_ONLY}",
        "NEO4J_TELEMETRY": "${user_config.NEO4J_TELEMETRY}",
        "NEO4J_LOG_LEVEL": "${user_config.NEO4J_LOG_LEVEL}",
        "NEO4J_LOG_FORMAT": "${user_config.NEO4J_LOG_FORMAT}",
        "NEO4J_SCHEMA_SAMPLE_SIZE": "${user_config.NEO4J_SCHEMA_SAMPLE_SIZE}",
        "NEO4J_MCP_EMBEDDING_PROVIDER": "${user_config.NEO4J_MCP_EMBEDDING_PROVIDER}",
        "NEO4J_MCP_EMBEDDING_CONFIGURATION": "${user_config.NEO4J_MCP_EMBEDDING_CONFIGURATION}"
      }
    }
  },
  "tools": [
    {
      "name": "get-schema",
      "description": "Introspect labels, relationship types, property keys"
    },
    {
      "name": "read-cypher",
      "description": "Execute arbitrary Cypher (read mode)"
    },
    {
      "name": "write-cypher",
      "description": "Execute arbitrary Cypher (write mode)"
    },
    {
      "name": "explain-cypher",
      "description": "Show the query plan for a Cypher statement without executing it"
    },
    {
      "name": "profile-cypher",
      "description": "Execute a Cypher statement and return results plus a profiled plan with runtime statistics"
    },
    {
      "name": "list-constraints-and-indexes",
      "description": "List the database's constraints and indexes"
    },
    {
      "name": "create-constraint",
      "description": "Create a uniqueness, key, or property-existence constraint"
    },
    {
      "name": "drop-constraint",
      "description": "Drop a constraint by name"
    },
    {
      "name": "create-index",
      "description": "Create a range, text, point, or lookup index"
    },
    {
      "name": "drop-index",
      "description": "Drop an index by name"
    },
    {
      "name": "list-gds-procedures",
      "description": "List GDS procedures available in the instance"
    },
    {
      "name": "vector-search",
      "description": "Run an approximate nearest-neighbor similarity search against a vector index"
    },
    {
      "name": "fulltext-search",
      "description": "Run a Lucene-backed full-text search against a fulltext index"
    },
    {
      "name": "create-vector-index",
      "description": "Create a vector index for similarity search"
    },
    {
      "name": "create-fulltext-index",
      "description": "Create a fulltext index for text search"
    },
    {
      "name": "set-vector-property",
      "description": "Set a node or relationship's embedding vector property"
    },
    {
      "name": "check-embedding-dimensions",
      "description": "Validate a configured embedding provider/model against a vector index's dimensions"
    }
  ],
  "compatibility": {
    "platforms": ["darwin", "linux", "win32"]
  },
  "keywords": [
    "Neo4j",
    "MCP",
    "Neo4j MCP",
    "Neo4j MCP Server",
    "Neo4j Official MCP Server",
    "neo4j-mcp"
  ],
  "license": "GPL",
  "repository": {
    "type": "git",
    "url": "https://github.com/neo4j/mcp"
  }
}
